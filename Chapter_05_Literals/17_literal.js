let age = "Akshay"; // String literal — a sequence of characters wrapped in double quotes
let isStudent = true; // Boolean literal — represents a logical true/false value
let pi = 3.14; // Number literal — a floating-point numeric value
let name = 'kumar'; // String literal — characters wrapped in single quotes
let nullValue = null; // Null literal — represents an intentional absence of any object value
let undefinedValue; // Undefined — variable declared but not assigned a value (undefined literal)



console.log(typeof age);
console.log(typeof pi);
console.log(typeof nullValue);
console.log(typeof undefinedValue);