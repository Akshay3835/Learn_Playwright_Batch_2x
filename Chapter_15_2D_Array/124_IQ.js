// what is size of the Matrix

let matrix_2D = [
    [1, 2, 3, 4],
];

console.log( matrix_2D );

// It is called these same rows and same columns - Grid

let grid = [
    [1, 2, 3],
    [2, 3, 4],
    [4, 5, 6]
];

console.log( `It is first row and first column` + " " + grid[0][0] );
console.log( `It is Second row and second column` + " " + grid[2][2] );
console.log( `It is Third row and second column` + " " + grid[2][1] );

console.log( grid[0].length ); // which gives column value


