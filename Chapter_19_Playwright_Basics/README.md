# Chapter 19 - Playwright Basics

Getting started with Playwright:
- Project setup and configuration
- Cross-browser projects (Chromium, Firefox, WebKit)
- Writing tests with `page.goto()` and `expect`
- HTML test reporter

```bash
npm install
npx playwright install
npx playwright test
```
