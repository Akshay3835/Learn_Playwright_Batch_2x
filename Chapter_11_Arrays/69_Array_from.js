// normal array basic creation

let team = ["India", "AUS", "WED", "ENG"];
console.log( team );


// Array using from interface for characters separation

let alpha = Array.from( "INDIA" );
console.log( alpha );

let name = Array.from( "AKSHAY" );
console.log( name );

let numbers = Array.from( "123456789" );
console.log( numbers );