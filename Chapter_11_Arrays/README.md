# Chapter 11 - Arrays

Array operations in JavaScript:
- Creation and access
- CRUD operations (add, remove, update)
- Searching and iteration
- Transform methods: `map`, `filter`, `reduce`, `flat`
- Sorting with `sort()`, slicing with `slice()`
