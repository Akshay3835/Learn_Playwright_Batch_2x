// normal array creation using new object

let numbers = new Array( 100, 200, 300, 500, 600 );
console.log( numbers );

// using of interface to Array

let num = Array.of( 100, 800, 900, 600 );
console.log( num );


let names = Array.of( "Akshay", "Abhi", "Srinija", "Cnu" );
console.log( names );

